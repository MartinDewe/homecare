import 'package:flutter/material.dart';

class Nursepage extends StatelessWidget {
  const Nursepage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Nurse Tab'));
  }
}